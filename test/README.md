# Jest Testing
This project uses Jest for unit and integration testing.

## Running Tests
```
npm run test
```

## Test Files
- Place test files in the `test/` folder or alongside source files with `.test.ts` or `.spec.ts` extensions.

## Configuration
See `jest.config.js` in each package for details.
