# Husky Git Hooks
This project uses Husky to enforce code quality and formatting before commits and pushes.

## Setup
After installing dependencies, run:

```
npx husky install
```

## Example Hooks
- pre-commit: Runs lint and format checks
- pre-push: Runs tests

See `.husky/` folder for hook scripts.
